## 용어 정리

- 매출: 수입
- 매입: 구매
- 지출: 매입을 제외한 지출
- 비용: 매입 + 지출
- 내역: 매출, 매입, 지출 기록
- 거래 유형: 매출, 매입, 지출 구분
- 카테고리: 거래 유형별 세부 항목
- 결제 방식: 현금, 카드, 계좌이체 등 결제 수단
- 프리셋: 자주 사용하는 카테고리, 결제 방식, 메모...등을 미리 등록해두는 기능  
				 혹은 항목 자체
- 프리셋 색상: 프리셋 버튼 구분 및 시각적 식별을 위한 속성

## 기능 요구사항 정리
1. 사이드 바 메뉴
   - 왼쪽 사이드바 메뉴 구성
     - 매출/비용 내역
     - 카테고리 관리
     - 자동화
     - 설정
     - 가이드
   - 프리셋 바(오른쪽 사이드 바) 메뉴 구성
     - 화살표 버튼 (사이드바 열기/닫기)
     - 프리셋 실행 버튼
     - 프리셋 수정모드 버튼
2. 매출/비용 내역
   - 내역 추가
		 - 매출/매입/지출 내역 추가
		 - 프리셋을 선택하면 자동으로 항목 채워짐
		 - 직접 입력 기능 (날짜, 금액, 메모 등)
   - 필터링 기능
     - 일일, 주간, 월간, 연간 필터링
     - 거래 유형 필터링
     - 결제 방식 필터링
     - 검색 기능 (메모/금액 범위 검색)
   - 내역 총합
     - 총 갯수
     - 총 금액
   - 내역 표시 항목
		 - 번호 (리스트 표시 용 순서 -> 갯수 파악 용)
		 - 거래 시간
		 - 거래 유형
		 - 결제 방식
		 - 거래 금액
		 - 메모
3. 자동화
	 - 반복 작업 자동화 설정
  	 - 매 주기마다 내역 추가
	 - 자동화 작업 목록 관리
  	 - 자동화 작업 추가
  	 - 자동화 작업 수정
  	 - 자동화 작업 삭제
6. 프리셋 바 - 오른쪽 사이드 바
   - 프리셋 목록
     - 등록한 프리셋 리스트 표시
     - 프리셋 선택 시 자동 내역 추가
     - 토스트 알림으로 내역 추가 확인
     - 토스트 알림에 되돌리기 버튼 포함
   - 프리셋 수정 모드
     - 프리셋 추가 버튼
       - 새 프리셋 등록 모달 창 열림
       - 프리셋 이름 입력
       - 프리셋 항목 설정 (거래 유형, 결제 방식, 금액, 메모 등)
			 - 프리셋 색상 선택
			 - 저장, 취소 버튼
     - 기존에 등록된 프리셋 관리 버튼
       - 각 프리셋 옆에 관리 버튼 표시
       - 프리셋 관리 버튼을 누르면, 모달 창이 열림
       - 프리셋 항목 수정 (이름, 항목, 색상 등)
			 - 저장, 취소 버튼
			 - 프리셋 삭제 (삭제 확인 모달 창 표시)
  			 - 삭제 확인, 취소 버튼
4. 설정
   - 테마 변경 (다크 모드, 라이트 모드)
   - 글자 크기 조절
     - 작음, 보통, 큼으로 조절 가능
   - 데이터 백업 및 복원
     - 수동 백업
       - 시스템 설정 백업
       - 데이터 백업
       - 시스템 설정 복원
       - 데이터 복원
     - 자동 백업 설정
       - 백업 주기 설정
       - 백업 위치 설정
       - 백업 파일 관리
       - 백업 알림 설정
   - 개발자용 옵션
     - 시스템 변수 보기
     - 로그 보기
   - 시스템 초기화
     - 초기화 경고 및 확인
     - 초기화 후 기본 설정 복원
     - 데이터 삭제 옵션
5. 가이드
   - 사용자 매뉴얼
   - 용어 설명
   - 개발자 정보


## 데이터 구조 정리

### 내역 원본

### 데이터 테이블

```sql
-- TallyCat SQLite Schema (v1)
PRAGMA foreign_keys = ON;

-- 1) Categories (태그: 카테고리)
-- - type_scope: INCOME / PURCHASE / EXPENSE 별로 카테고리 분리
-- - sort_order: 사용자 정렬(드래그앤드롭) 저장
-- - is_active: 삭제 대신 비활성화
CREATE TABLE IF NOT EXISTS categories (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  type_scope TEXT NOT NULL CHECK (type_scope IN ('INCOME','PURCHASE','EXPENSE')),
  sort_order INTEGER NOT NULL DEFAULT 0,
  is_active INTEGER NOT NULL DEFAULT 1,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now')),
  UNIQUE(name, type_scope)
);

CREATE INDEX IF NOT EXISTS idx_categories_scope_order
  ON categories(type_scope, sort_order);

CREATE INDEX IF NOT EXISTS idx_categories_active
  ON categories(is_active);

-- 2) Payment Methods (태그: 결제 방식)
CREATE TABLE IF NOT EXISTS payment_methods (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL UNIQUE,
  sort_order INTEGER NOT NULL DEFAULT 0,
  is_active INTEGER NOT NULL DEFAULT 1,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE INDEX IF NOT EXISTS idx_payment_methods_order
  ON payment_methods(sort_order);

CREATE INDEX IF NOT EXISTS idx_payment_methods_active
  ON payment_methods(is_active);

-- 3) Presets (오른쪽 프리셋 바에서 실행되는 템플릿)
-- - type: 거래 유형 (필수)
-- - category_id / payment_method_id: 선택
-- - amount: 기본 금액(선택)
-- - memo_template: 기본 메모(선택)
-- - color: 프리셋 버튼 색상(선택, hex string 권장)
-- - sort_order: 프리셋 버튼 정렬 저장
CREATE TABLE IF NOT EXISTS presets (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  type TEXT NOT NULL CHECK (type IN ('INCOME','PURCHASE','EXPENSE')),
  category_id INTEGER NULL,
  payment_method_id INTEGER NULL,
  amount INTEGER NULL CHECK (amount IS NULL OR amount >= 0),
  memo_template TEXT NULL,
  color TEXT NULL,
  sort_order INTEGER NOT NULL DEFAULT 0,
  is_active INTEGER NOT NULL DEFAULT 1,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now')),
  FOREIGN KEY (category_id) REFERENCES categories(id) ON UPDATE CASCADE,
  FOREIGN KEY (payment_method_id) REFERENCES payment_methods(id) ON UPDATE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_presets_type_order
  ON presets(type, sort_order);

CREATE INDEX IF NOT EXISTS idx_presets_active
  ON presets(is_active);

-- 4) Transactions (내역 원본)
-- - occurred_at: 거래 시간(ISO datetime 문자열 권장)
-- - type: 거래 유형(필수)
-- - category_id / payment_method_id: 선택
-- - amount: 원 단위 정수(권장)
CREATE TABLE IF NOT EXISTS transactions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  occurred_at TEXT NOT NULL,
  type TEXT NOT NULL CHECK (type IN ('INCOME','PURCHASE','EXPENSE')),
  category_id INTEGER NULL,
  payment_method_id INTEGER NULL,
  amount INTEGER NOT NULL CHECK (amount >= 0),
  memo TEXT NULL,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now')),
  FOREIGN KEY (category_id) REFERENCES categories(id) ON UPDATE CASCADE,
  FOREIGN KEY (payment_method_id) REFERENCES payment_methods(id) ON UPDATE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_transactions_occurred_at
  ON transactions(occurred_at);

CREATE INDEX IF NOT EXISTS idx_transactions_type
  ON transactions(type);

CREATE INDEX IF NOT EXISTS idx_transactions_category_id
  ON transactions(category_id);

CREATE INDEX IF NOT EXISTS idx_transactions_payment_method_id
  ON transactions(payment_method_id);
```

### 시스템 설정 데이터 기본값

```json
{
  "theme": "dark",
  "font_size": 1,
  "backup_alert_interval_days": 7,
  "auto_backup": {
    "active": true,
    "interval_days": 3,
    "path": "/Users/you/Backups/TallyCat",
    "file_name": "tallycat-backup-{YYYY-MM-DD_HH-MM-SS}.backup",
    "file_limit_count": 10
    }
}
```

### 데이터 내보내기시 구조
#### JSON 구조
```json
{
  "schema_version": 1,
  "app_version": "0.1.0",
  "exported_at": "2026-01-16T11:23:45+09:00",

  "settings": {
  "theme": "dark",
  "font_size": 1,
  "backup_alert_interval_days": 7,
  "auto_backup": {
    "active": true,
    "interval_days": 7,
    "path": "/Users/you/Backups/TallyCat",
    "file_name": "tallycat-backup-{YYYY-MM-DD_HH-MM-SS}.backup",
    "file_limit_count": 10
    }
  },

  "data": [],
  "presets": [],
  "categories": [],
  "payment_methods": []
}
```

## 개발 진행시 주의사항

1. SOLID 원칙 준수
2. 개발자가 읽기 쉽도록 코드 작성
3. 주석은 필수로 작성
4. 코드가 길고 복잡해지면, 함수와 클래스로 분리하고 파일을 나눈다.
5. 기능을 수행하는 함수는 한 가지 일만 하도록 한다.
6. 구조, 기능에 대해 설명하는 문서를 작성한다.
7. 에러 처리/로그 남기기 규칙(IPC/파일/DB는 반드시 try/catch + 로그)
8. 타입 정의 우선(export JSON, DB row 타입을 shared/types로 고정)
9. 한가지 기능씩 진행하고, 확인을 요청한다.
10. Task List를 만들어서 진행하고, 진행이 끝날때마다 체크해서 진행 상황을 공유한다.
11. 만약 작업중 새롭게 추가되거나 변경되야하는 사항이 있다면 바로 공유하고, Task List를 업데이트한다.